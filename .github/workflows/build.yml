name: Build and Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Create dist directory
      run: mkdir -p dist
      
    - name: Build and minify
      run: npm run build
      
    - name: Verify build
      run: |
        echo "✅ Build successful!"
        echo "📦 Build output:"
        ls -la dist/
        echo ""
        echo "📊 File sizes:"
        echo "Original: $(wc -c < src/index.js) bytes"
        echo "Minified: $(wc -c < dist/open-web-inspector.min.js) bytes"
        ORIGINAL_SIZE=$(wc -c < src/index.js)
        MINIFIED_SIZE=$(wc -c < dist/open-web-inspector.min.js)
        echo "Compression ratio: $MINIFIED_SIZE bytes (minified) vs $ORIGINAL_SIZE bytes (original)"
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-artifacts
        path: |
          dist/
          !dist/*.map
        retention-days: 30
